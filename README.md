# CHAL-ESP-IDF
### Platform dependant code that connects up the Arduino SDK to the CHAL and CSAL shared libraries. 

### Anything to support the hardware features that utilize the Arduino platform. 

Note: Generally you should avoid using these features since you are essentially calling an abstraction on an abstraction, Arduino is mostly a fallback